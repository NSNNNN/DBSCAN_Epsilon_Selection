# DBSCAN_Epsilon_Selection

# Choosing an Efficient Epsilon for DBSCAN

در الگوریتم **DBSCAN**، پارامتر **ε (epsilon)** شعاع همسایگی است و انتخاب درست آن تأثیر مستقیم بر کیفیت خوشه‌بندی دارد.  
در این پروژه، دو روش کارآمد برای تعیین مقدار مناسب ε معرفی و با یک مثال عملی پیاده‌سازی می‌شوند.

---

## 🔹 روش اول: نمودار فاصله k-Nearest Neighbors (k-distance plot)

### ایده
برای هر نقطه، فاصله آن تا k‌اُمین نزدیک‌ترین همسایه محاسبه می‌شود  
(k معمولاً برابر `min_samples` است).  
سپس این فاصله‌ها به‌صورت صعودی رسم می‌شوند.  
نقطه شکست یا **Elbow** بهترین مقدار ε را نشان می‌دهد.

### مراحل
1. انتخاب مقدار `min_samples` (معمولاً 4 یا 5)  
2. محاسبه فاصله هر نقطه تا k‌اُمین همسایه  
3. مرتب‌سازی فاصله‌ها  
4. رسم نمودار و پیدا کردن نقطه شکست

---

## 🔹 روش دوم: تحلیل پایداری خوشه‌بندی

### ایده
DBSCAN با چند مقدار مختلف ε اجرا می‌شود.  
مقداری از ε مناسب است که:

- تعداد خوشه‌ها نسبتاً پایدار باشد  
- نویز (outlier) خیلی زیاد نشود  
- ساختار خوشه‌ها معنی‌دار باقی بماند
